mixin dropdown(params)
  .dropdown.mb-3
    select.dropdown__field(
      name=params.name,
      required=params.required)
      option(disabled, selected, value="")= params.placeholder
      if params.options
        each option in params.options
          option.dropdown__option(value=option.value)=option.name

